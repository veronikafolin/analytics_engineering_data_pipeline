select
	TEST_UNIQUE_ID,
	MODEL_UNIQUE_ID,
	DATABASE_NAME,
	SCHEMA_NAME,
	TABLE_NAME,
	COLUMN_NAME,
	TEST_RESULTS_QUERY,
	STATUS,
	FAILURES,
	RESULT_ROWS,
	FAILED_ROW_COUNT

from {{ source('elementary', 'elementary_test_results') }}